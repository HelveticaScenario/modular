{
  "name": "modular_web_client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.tsx --bundle --minify --platform=neutral --sourcemap --target=es2020 --outdir=dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.2.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "esbuild": "^0.8.34",
    "preact": "^10.5.11",
    "tailwind": "^4.0.0"
  }
}
