// Backward-compat barrel — actual definitions live in `./graph/`.
// Splitting performed in PR 2 of the autogen-dsl migration; full file moves to
// `@modular/dsl` in PR 3 and this barrel goes away.
export * from './graph';
